spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mdd
    username: root
    password: Maroc241996.

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

server:
  port: 8080

jwt:
  secret: mySuperSecretKeyForJwtAuthenticationMDDProject
  expiration: 86400000